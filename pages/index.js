export default function Home() {
  return (
    <div style={{ textAlign: 'center', padding: '50px' }}>
      <h1>Stow Mock API</h1>
      <p>This is a mock API for simulating responses from <code>qastg.api.stow-protocol.com</code>.</p>
      <p>Try accessing <a href="/api/stow">/api/stow</a> to see the API response.</p>
    </div>
  );
}
